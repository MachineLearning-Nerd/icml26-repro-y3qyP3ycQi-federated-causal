# Legacy verifier

`verify_fc_toy.py` is preserved for commit-history provenance only. It is a
small 200-patient proxy that predates the current claim contracts and must not
be used as evidence for the paper. The authoritative entry point is
`../run.sh`, which runs `../src/run_all.py`.
