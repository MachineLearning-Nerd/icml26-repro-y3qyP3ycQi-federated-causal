"""Archived marker for the superseded toy verifier.

The executable toy verifier is retained in the pre-cleanup Git history. This
file is deliberately non-executable so it cannot be mistaken for evidence.
Use ``../run.sh`` for the current verifier.
"""

raise SystemExit(
    "This verifier is archived and is not publication evidence; run bash repro/run.sh instead."
)
